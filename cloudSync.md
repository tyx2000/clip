# Cloud Sync 可实现方案

## 1. 目标

在录制确认弹窗开启“云端同步”后，系统需要同时满足下面四个目标：

1. 本地录制始终优先，网络异常不能中断录制。
2. 分片在弱网、丢包、高延迟、服务端短暂失败下仍可最终上传成功。
3. 云端能够基于分片做可靠合并，且结果与本地最终视频一致。
4. 在云端明确成功之前，本地分片不能被过早清理，避免无法补传。

这意味着云同步不能设计成“边录边依赖网络”，而必须设计成：

- 先本地稳定落盘
- 再后台异步上传
- 再由云端确认会话完成并合并
- 最后清理本地分片

## 2. 当前问题

当前项目已经具备本地分片、合并、清理的基础能力，但如果直接把“云同步”叠加在现有录制流程上，会遇到下面的问题：

1. 录制线程和上传线程耦合过深时，网络抖动会反向影响录制稳定性。
2. 只靠内存状态无法支撑断网恢复、重启恢复、崩溃后续传。
3. 分片上传如果没有幂等机制，弱网超时会导致服务端重复写入。
4. 如果没有完整性校验，服务端可能拿到截断文件或损坏分片。
5. 如果客户端在本地合并完成后立即清理分片，云端失败时就失去补传能力。
6. 如果云端在分片未齐时就开始合并，最终文件会缺片或乱序。

## 3. 总体架构

建议将链路拆成三个解耦层：

### 3.1 录制层

职责：

- 生成完整分片
- 本地写盘
- 更新本地 manifest

原则：

- 不等待网络
- 不关心云端是否成功
- 只保证本地会话完整可恢复

### 3.2 同步层

职责：

- 扫描待上传分片
- 排队上传
- 重试失败片
- 更新本地上传状态

原则：

- 后台异步执行
- 严格幂等
- 与录制线程解耦

### 3.3 汇总层

职责：

- 服务端验收分片
- 校验完整性
- 判断会话是否可合并
- 合并输出云端成片

原则：

- 仅在所有分片齐全时合并
- 合并流程可重入
- 最终状态明确可查询

## 4. 建议状态机

### 4.1 客户端会话状态

建议为每个录制会话维护以下状态：

- `recording`
- `stopping`
- `stopped`
- `uploading`
- `uploaded`
- `merge_pending`
- `merged`
- `failed`

说明：

- `recording`: 正在录制，分片不断生成。
- `stopping`: 用户已停止，正在完成最后分片。
- `stopped`: 本地录制结束，等待上传或等待云端处理。
- `uploading`: 还有分片待上传或正在上传。
- `uploaded`: 全部分片已成功上传。
- `merge_pending`: 服务端已具备合并条件，等待或正在合并。
- `merged`: 云端合并完成。
- `failed`: 会话失败，需要人工介入或重试。

### 4.2 客户端分片状态

建议每个 segment 单独维护状态：

- `pending`
- `uploading`
- `uploaded`
- `failed`
- `abandoned`

含义：

- `pending`: 已本地落盘，待上传。
- `uploading`: 已进入上传任务。
- `uploaded`: 服务端已确认接收并校验通过。
- `failed`: 上传失败，等待重试。
- `abandoned`: 会话被用户取消，分片不再补传。

### 4.3 服务端会话状态

建议服务端单独维护：

- `receiving`
- `ready_to_merge`
- `merging`
- `merged`
- `merge_failed`

## 5. 本地 manifest 设计

建议每个录制会话都持久化一个 manifest 文件，放在会话目录下，与现有本地 session 结构兼容。

建议字段：

```json
{
  "version": 2,
  "sessionId": "session-20260330-220001-abcd12",
  "cloudSyncEnabled": true,
  "status": "recording",
  "mimeType": "video/webm",
  "extension": "webm",
  "segmentDurationMs": 5000,
  "startedAt": 1711800000000,
  "stoppedAt": null,
  "totalBytes": 0,
  "localOutput": {
    "path": "",
    "status": "pending",
    "bytes": 0,
    "durationSec": null
  },
  "remote": {
    "uploadStatus": "pending",
    "mergeStatus": "pending",
    "sessionToken": "",
    "lastError": "",
    "updatedAt": 0
  },
  "segments": [
    {
      "index": 1,
      "path": "segment-0001.webm",
      "startedAt": 1711800000000,
      "endedAt": 1711800005000,
      "bytes": 2458812,
      "checksum": "sha256:...",
      "uploadStatus": "uploaded",
      "retryCount": 1,
      "remoteObjectKey": "sessions/xxx/segment-0001.webm",
      "etag": "..."
    }
  ]
}
```

## 6. 分片策略

### 6.1 分片时长

建议默认 5 秒。

原因：

- 失败时重传成本可控
- 对弱网更友好
- 每片仍有足够内容，不会请求过于频繁

后续可支持配置，但第一阶段不建议做自适应分片。

### 6.2 分片规则

开启云同步时：

- 每 5 秒形成一个完整 segment blob
- segment 先写入本地临时文件
- 本地文件 finalize 后更新 manifest
- 再把该 segment 推入上传队列

关闭云同步时：

- 继续保留本地连续录制或本地单会话写盘模式
- 不进入云端上传链路

## 7. 上传设计

### 7.1 原则

- 上传必须异步
- 上传不能阻塞录制
- 上传结果必须落 manifest
- 上传操作必须幂等

### 7.2 上传并发

第一阶段建议：

- 单会话并发数：`1`
- 全局并发数：`2`

理由：

- 弱网下高并发会抢占带宽并增加重试概率
- 先追求稳定，不追求极限吞吐

### 7.3 上传重试

失败后指数退避：

- 第 1 次：2 秒
- 第 2 次：5 秒
- 第 3 次：10 秒
- 第 4 次：30 秒
- 第 5 次：60 秒

最大重试次数建议：

- 普通错误：5 次
- 网络离线：不算失败次数，进入暂停队列，等待恢复

### 7.4 超时策略

每个分片上传建议有单独超时：

- 默认 30 秒
- 若分片较大可放宽到 60 秒

超时后：

- 客户端视为未知结果
- 不能直接假定服务端没收到
- 必须依靠幂等键做安全重试

## 8. 幂等设计

服务端必须支持幂等上传。

建议每个分片使用以下身份：

- `sessionId`
- `segmentIndex`
- `checksum`

这样服务端收到重复提交时：

- 若对象已存在且 checksum 一致，直接返回成功
- 若对象已存在但 checksum 不一致，返回冲突错误

客户端好处：

- 超时后可安全重试
- 重试不会生成重复分片
- 可以支持重启后续传

## 9. 完整性校验

客户端在分片落盘后计算：

- `size`
- `sha256`

服务端接收分片后校验：

- 文件大小是否一致
- checksum 是否一致

只有校验成功，服务端才将该分片标记为已接收。

如果校验失败：

- 返回明确错误码
- 客户端将该分片状态改为 `failed`
- 后续重新上传整片

## 10. 服务端接口建议

当前 `server/index.js` 还是空实现，建议服务端按下面接口设计。

### 10.1 创建云同步会话

`POST /api/cloud-sync/sessions`

请求：

```json
{
  "sessionId": "session-20260330-220001-abcd12",
  "mimeType": "video/webm",
  "extension": "webm",
  "segmentDurationMs": 5000,
  "startedAt": 1711800000000
}
```

返回：

```json
{
  "ok": true,
  "sessionId": "session-20260330-220001-abcd12",
  "uploadToken": "signed-or-session-token"
}
```

### 10.2 上传单个分片

`PUT /api/cloud-sync/sessions/:sessionId/segments/:index`

请求头建议：

- `Content-Type: video/webm`
- `X-Checksum-Sha256`
- `X-File-Size`

返回：

```json
{
  "ok": true,
  "sessionId": "session-20260330-220001-abcd12",
  "segmentIndex": 1,
  "etag": "remote-etag",
  "alreadyExisted": false
}
```

### 10.3 通知录制结束

`POST /api/cloud-sync/sessions/:sessionId/complete`

请求：

```json
{
  "stoppedAt": 1711800030000,
  "segmentCount": 6,
  "totalBytes": 12499881
}
```

作用：

- 告诉服务端不会再有新分片
- 服务端检查是否全部接收完成
- 满足条件则进入 `ready_to_merge`

### 10.4 查询云端状态

`GET /api/cloud-sync/sessions/:sessionId`

返回：

```json
{
  "ok": true,
  "sessionId": "session-20260330-220001-abcd12",
  "uploadStatus": "uploading",
  "mergeStatus": "merging",
  "uploadedSegments": 5,
  "totalSegments": 6,
  "remoteVideoUrl": "",
  "lastError": ""
}
```

## 11. 本地清理策略

### 11.1 绝不能做的事

云端同步开启后，不能在以下时机清理本地分片：

- 本地录制刚结束
- 本地合并刚完成
- 分片刚上传一部分

因为任何一个时刻都可能出现：

- 还有分片未上传
- 云端尚未完成合并
- 服务端状态未知

### 11.2 推荐清理条件

只有同时满足下面条件时才允许清理本地分片：

1. 本地完整视频已生成
2. 服务端所有分片已接收
3. 服务端云端合并完成
4. 客户端已拿到服务端成功状态

### 11.3 保守策略

即使满足上面条件，也建议保留一个延迟窗口，例如：

- 延迟 24 小时清理分片
- 或只清理中间分片，保留最终本地视频

这样可以处理：

- 服务端后验发现视频损坏
- 用户投诉云端回放异常
- 需要人工重新补传

## 12. 弱网场景处理

### 12.1 高延迟

问题：

- 请求长时间不返回
- 客户端不确定是否成功

方案：

- 上传超时后进入“未知结果重试”
- 使用幂等键确保重试安全
- 不阻塞后续录制

### 12.2 丢包

问题：

- 分片可能上传不完整

方案：

- 服务端按 checksum 校验
- 校验失败直接拒绝
- 客户端重传整片

### 12.3 短暂断网

问题：

- 连续几片上传失败

方案：

- 分片继续本地落盘
- 上传队列暂停
- 网络恢复后自动恢复队列

### 12.4 长时间离线

问题：

- 用户录完后很久都没网

方案：

- 本地视频和分片保留
- manifest 保持 `pending/failed`
- 下次打开应用后继续补传

### 12.5 应用崩溃或重启

问题：

- 内存队列全部丢失

方案：

- 依赖 manifest 扫描未完成会话
- 恢复本地状态
- 恢复待上传分片队列
- 重新拉起上传任务

## 13. 边界情况

### 13.1 最后一片不足 5 秒

处理：

- 仍然作为合法分片上传
- 不能因为时长不足 5 秒就丢弃

### 13.2 分片 0 字节

处理：

- 不上传
- 标记异常
- 写入日志
- 若为停止边界触发的空片，可直接忽略

### 13.3 服务端已收到但客户端超时

处理：

- 重试上传
- 服务端基于幂等键直接返回已存在

### 13.4 客户端顺序上传失败导致乱序

处理：

- 服务端必须按 `segmentIndex` 排序合并
- 不依赖到达顺序

### 13.5 部分分片永久失败

处理：

- 会话保持 `failed`
- UI 显示“云同步失败，可重试”
- 本地视频仍可正常使用

### 13.6 本地视频成功但云端失败

处理：

- 用户仍能看到本地视频
- 云端状态显示失败
- 提供“继续补传”或“重新同步”入口

### 13.7 用户手动删除本地分片

处理：

- 如果云端未完成，标记会话不可恢复
- 提示用户云端同步将无法继续

## 14. UI 建议

开启云同步后，建议显示三类状态：

1. 本地录制状态
2. 上传状态
3. 云端合并状态

示例：

- `录制中`
- `待上传 2 / 上传中 1 / 失败 0`
- `云端处理中`
- `云端已完成`

不要把这些状态混成一个“保存中”，否则用户无法判断到底是哪一步慢。

## 15. 客户端实现建议

第一阶段建议只实现以下能力：

1. 本地 manifest 持久化
2. 本地分片写盘
3. 后台上传队列
4. segment 级重试
5. 服务端幂等接收
6. 云端齐片后合并
7. 云端成功后再清理本地分片

暂时不建议做：

- 分片内部断点续传
- 多会话高并发优化
- 动态分片时长
- 上传带宽自适应

## 16. 服务端落地建议

当前 `server/index.js` 为空，建议第一版服务端至少实现：

1. `POST /sessions`
2. `PUT /sessions/:id/segments/:index`
3. `POST /sessions/:id/complete`
4. `GET /sessions/:id`
5. 本地磁盘或对象存储持久化
6. 基于 ffmpeg 的云端合并任务

如果第一版不接对象存储，也可以先用本地磁盘目录模拟：

- `uploads/<sessionId>/segment-0001.webm`
- `uploads/<sessionId>/manifest.json`
- `uploads/<sessionId>/merged.webm`

等协议跑通后再换 S3、OSS、COS 等对象存储。

## 17. 推荐实施顺序

### 阶段一：协议跑通

- 定义会话与分片接口
- 客户端写 manifest
- 服务端接收分片
- 实现分片幂等

### 阶段二：稳定性补强

- 客户端重试队列
- 离线暂停与恢复
- 崩溃恢复
- 云端合并状态查询

### 阶段三：体验增强

- 上传进度显示
- 云端失败重试按钮
- 清理策略配置
- 监控与日志

## 18. 成功标准

完成后应满足以下验收标准：

1. 弱网下录制不断流，本地视频完整可用。
2. 断网后恢复网络，可以继续补传未完成分片。
3. 服务端不会因为重复请求生成重复分片。
4. 云端最终视频时长与本地接近一致。
5. 在云端明确成功前，本地分片不会被误删。
6. 应用重启后，未完成会话能继续同步。

## 19. 最终结论

可行，而且建议立即采用“本地优先、异步上传、幂等接收、云端齐片合并、成功后延迟清理”的方案。

这是目前面对弱网、丢包、高延迟时最稳、实现复杂度也合理的设计。与现有项目的本地 session 结构兼容度也高，适合作为下一阶段的正式重构目标。
